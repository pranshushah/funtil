/**
 * @description returns negation of given number.
 * @category Math
 * @example
 * ```typescript
 * F.negate(33) // -33
 * F.negate(-21) // 21
 * ```
 */
export function negate(x: number) {
  return -x;
}
