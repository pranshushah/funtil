/**
 * @description always returns true.
 * @category Function
 * @example
 * F.T(33) // true
 * F.T() // true
 */
export function T(x?: any): true {
  if (x) {
    return true;
  } else {
    return true;
  }
}
